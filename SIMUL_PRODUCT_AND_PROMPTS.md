# Lay-Z / Lay-몽 시뮬 — 소비자 구조·목적·프롬프트 정리

> 기준 코드: `3차_시뮬_layz_test.html`, `3차_시뮬_꿈해몽_산하.html`, 셸 `index.html`  
> “프롬프트”는 **LLM에 넣는 문자열**과, Lay-Z처럼 **질문·선지가 곧 입력 설계인 경우**를 구분해 적었습니다.

---

## 1. Lay-Z력 테스트 (Lay-Z)

### 1-1. 소비자에게 보이는 흐름(구조)

1. **인트로** — 오늘 이미 측정했는지(로컬 기록), 평균·누적·(데모) 상위 %·로그인 적립 카운트 등 요약.
2. **8문항 설문** — 각 문항마다 4지선다. 문항·선지·점수 가중치·라벨(예: 수면 유형 코드)은 코드에 고정.
3. **결과** — **Lay-Z 지수(0~99)만** `calcScore()`로 확정 → `getTier()`로 **색·구간 토큰(TIERS)** 적용 → 화면은 먼저 TIERS 카피로 그린 뒤, **`/api/openai-dev`** 로 티어명·태그·헤드·본문·추천 3개·수면 rx JSON을 받아 DOM·공유용 `st.lzTierForShare`에 반영(실패 시 TIERS 폴백).
4. **내 기록** — `localStorage`의 측정 이력 목록·최근 7일 막대(참고용 문구 병기).

### 1-2. 제품이 소비자에게서 “얻고자 하는 것”

| 단계 | 수집·생성되는 것 |
|------|------------------|
| 설문 | 하루 컨디션에 대한 **자기보고(선택)** — 기상·멘탈·신체·수면 등 라이프스타일 스냅샷 |
| 결과 | **점수만** 규칙 기반 · **문장·추천·수면 카피**는 API 생성(폴백 TIERS) |
| 기록 | **리텐션**용 로컬 히스토리(날짜·점수·티어·일부 키워드) |

즉, **개인 서사 입력(자유 서술)은 거의 없고**, “오늘 나는 어떤 상태인가”를 **빠르게 고르게** 하는 구조입니다.

### 1-3. 목적(추정 의도)

- **낮은 마찰로 매일/자주 열리게** 하고, **공유 가능한 결과 카드**로 바이럴·브랜드 터치를 노림.
- 장기적으로는 **게이미피케이션**(누적·순위·적립 등)과 연결해 **습관 측정 앱** 같은 포지션을 열어두는 단계로 보는 것이 자연스럽습니다.

### 1-4. “프롬프트”에 해당하는 것

**점수(숫자)**  
- **`Qs` 배열** + **`calcScore()`** — LLM 없음. (추후 백엔드에서 동일 규칙으로 재계산·저장한다는 전제와 맞춤.)

**색·레이아웃 토큰**  
- **`getTier()` / `TIERS`** — 점수 구간별 accent·bg·meter 등 **디자인 고정값**. AI가 이 색값을 바꾸지 않습니다.

**사용자에게 보이는 카피(티어 이름, 헤드, 본문, 추천 3개, 수면 rx)**  
- **`fetchLayZCopy`** → `POST /api/openai-dev`  
- **system (`layzSystem`, 줄 단위 join)**  
  1. 브랜드 "Lay-Z" 오늘 컨디션 카피 작가.  
  2. 말투·금지어(진부 운세체, 번역투, 과한 한자어).  
  3. 의학·법률·투자·진단 단정 금지; 점수는 이미 확정.  
  4. JSON 한 덩어리만, 바깥 텍스트·마크다운·코드펜스 금지.  
  5. Lay-Z 지수 숫자는 주어진 값이며 변경·재해석 금지.  
- **user** — 고정 Lay-Z 지수, 참고 티어 id/기본이름/태그, 8문항 요약(문항별 선택 한 줄씩), 그리고 JSON 스키마 지시:  
  `tierName`, `tierTag`, `head`, `desc`, `recs`×3(`tag`,`title`,`desc`), `rx`(`title`,`sub`,`items`×3).  
- **파라미터:** `max_tokens: 1400`, `temperature: 0.7`  
- **폴백:** HTTP 오류·JSON 파싱 실패·필드 검증 실패 시 **TIERS 원본** 유지.

셸의 **`laylay-dev-sync`** 는 Lay-몽과 같이 **키·모델**을 iframe `sessionStorage`에 넣고, `laylayEffectiveApiKey` / `laylayEffectiveModel` 로 Lay-Z 호출에 재사용합니다.

### 1-5. 아마의도(해석)

> “점수는 **제품·백엔드와 공유할 단일 규칙**으로 고정하고, 그 주변 카피만 LLM으로 풀어 **매번 다른 맛**을 낸다. 실패해도 TIERS로 깨지지 않게 한다.”

---

## 2. Lay-몽 (꿈 해몽)

### 2-1. 소비자에게 보이는 흐름(구조)

1. **입력 단계** — 양력/음력, 성별, 생년월일, 태어난 시간, 꿈 본문(300자), 꿈에 대한 기분.
2. **로딩** — API 대기 UI.
3. **결과 탭** — “내 결과”: 링 점수·운세 카드·행운·해시태그 등. “레이도감”: 피드(로컬 + 시드).
4. **부가 UI(선반영)** — 유의사항 문구, 내 이전 해몽 / 다른 해몽 미리보기, 생년 옆 `?` 도움말 모달 등.

### 2-2. 제품이 소비자에게서 “얻고자 하는 것”

| 입력 | 용도 |
|------|------|
| 생년월일·시간·성별 | **맥락(사주·운세 톤)** 을 프롬프트에 넣기 위한 메타 |
| 꿈 텍스트 | **해몽의 핵심 입력** |
| 기분 | 결과 톤·카피 일관성 보조 |

산출은 **구조화된 JSON 한 덩어리**(별점 + 설명 + 한줄평 + 행운 3종 + 해시태그)로 고정해, UI가 파싱해 뿌립니다.

### 2-3. 목적(추정 의도)

- **감정 소비·공유**가 되는 “오늘의 꿈” 코너로 **체류·재방문**을 노림.
- **개인정보(생년)**는 민감하므로, 화면에 **용도·보관 방식**을 설명하고(유의사항·`?` 모달), 실제 서비스에서는 **정책·동의·백엔드**와 맞춰야 합니다.

### 2-4. LLM 프롬프트 정리 (코드 기준)

**호출:** `POST /api/openai-dev`  
**바디 요약:** `apiKey`, `model`, `messages`, `max_tokens: 2400`, `temperature: 0.68`

#### System (`laymongSystem` — 줄바꿈으로 join)

1. 당신은 브랜드 **"Lay-몽"**의 꿈 해석 캐릭터.
2. 말투: 밤에 친한 사람에게 카톡하듯 따뜻하고 리듬 있게. 진부한 운세 멘트·번역투·과한 한자어 나열은 피함.
3. 사용자 꿈 속 **구체 단어·사건·풍경**을 각 설명마다 최소 한 번은 인용/짚어 **개인 맞춤**처럼 읽히게.
4. 가벼운 비유·한 줄 농은 가능하나 농담만으로 끝내지 않음. 재물/애정/직장/건강은 **서로 다르게** 서술.
5. 출력은 **요청된 JSON 한 덩어리만**. JSON 바깥 텍스트·마크다운·코드펜스 금지.
6. 운세는 재미·상징이며 의학·법률·투자 조언이 아님 — **단정적 단언** 지양.

#### User (`prompt` — 템플릿)

- 한국어 지시: “다음 정보를 바탕으로 해몽해줘.”
- 삽입 필드: `cal`, `year`, `month`, `day`, `gender`, `time`, `mood`, `moodEmoji`, `dream`(삼중따옴표로 감쌈).
- **JSON 스키마**와 키 이름을 명시하고, 각 운 설명·총론은 2~4문장·해요체, 꿈 내용 인용, **한줄평** 규칙(1문장·길이 권장·이미지/비유·상투·‘오늘은’ 남발 금지), **행동해시태그** 5개 규칙 등을 텍스트로 반복 지시.
- **별점 규칙**: 재물·애정·직장·건강 각 1~5 정수.

실패 시(HTTP 오류·JSON 파싱 실패 등)에는 코드에 박힌 **`MOCK` 상수**로 동일 필드를 채워 UI를 깨지 않게 합니다.

### 2-5. 아마의도(해석)

> “꿈이라는 **저마찰 입력**으로 감정 이입을 만들고, 생년·기분을 얹어 **‘나만의 운세’ 같은 몰입**을 준다. 출력은 **JSON으로 고정**해 제품·알림·피드 확장을 쉽게 하고, 톤 가이드로 **브랜드 캐릭터 일관성**을 잡는다.”

---

## 3. 한눈에 비교

| 항목 | Lay-Z | Lay-몽 |
|------|-------|--------|
| 핵심 입력 | 8문항 객관식 | 꿈 서술 + 프로필·기분 |
| LLM | **점수 제외** 카피·추천·수면 문구 → `/api/openai-dev` | `/api/openai-dev`로 해몽 JSON |
| 산출 | 규칙 **점수** + (성공 시) 모델 JSON → UI·공유 | 모델 JSON → 카드 UI |
| 저장 | `layz5:hist` 등 로컬 | 피드 `laymong_feed_v2` 등 로컬 |

---

## 4. 참고

- 실제 **개인정보 처리방침·동의 문구**는 법무/기획 확정본으로 교체해야 하며, 본 문서는 **현재 시뮬 코드 관점**의 정리입니다.
- Lay-Z의 “상위 %”·“로그인 적립” 등은 **데모/로컬 추정** 성격이 강하니, 라이브 서비스에서는 별도 명세가 필요합니다.
